# AUTOMATICALLY GENERATED FILE, DO NOT EDIT MANUALLY.
# Generated by AWS CDK and [cdk-pipelines-github](https://github.com/cdklabs/cdk-pipelines-github)

name: CI/CD Pipeline
on:
  push:
    branches:
      - main
  workflow_dispatch: {}
jobs:
  Build-Build:
    name: Synthesize
    permissions:
      contents: read
      id-token: write
    runs-on: ubuntu-latest
    needs: []
    env: {}
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - run: env
      - run: export tag=latest
      - name: Build
        run: |-
          cd flask-docker-app
          docker build -t $ecr_repo_uri:$tag .
          $(aws ecr get-login --no-include-email)
          docker push $ecr_repo_uri:$tag
      - run: echo "in post-build stage"
      - run: cd ..
      - run: printf '[{"name":"flask-app","imageUri":"%s"}]' $ecr_repo_uri:$tag >
          imagedefinitions.json
      - run: pwd; ls -al; cat imagedefinitions.json
      - name: Upload cdk.out
        uses: actions/upload-artifact@v3
        with:
          name: cdk.out
          path: cdk.out
